import DefaultLayout from '../layouts/MainLayout'

const ExpertiseLayout = ({ children }: { children: React.ReactNode }) => (
  <DefaultLayout>{children}</DefaultLayout>
)
export default ExpertiseLayout
